name = "versewell-api"
main = "index.js"
compatibility_date = "2024-09-01"
workers_dev = true

# Static-only architecture: no D1, no KV, no bindings. The Worker reads the
# static JSON tree served by Cloudflare Pages at request time.
